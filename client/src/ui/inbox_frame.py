"""Message list — inbox + sent tabs.

PLANNED — to be extracted from ``main_frame.py`` (the conversation
sidebar and thread list currently live there). Once the service layer is
wired up this frame will render :class:`~secure_messenger_client.models.message.Message`
objects returned by ``MessageService.inbox`` / ``.sent``.
"""
