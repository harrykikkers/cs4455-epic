"""Message list — inbox + sent tabs.

PLANNED — to be extracted from ``main_frame.py`` (the conversation
sidebar and thread list currently live there). It will render the message
records returned by ``MessageService.inbox`` / ``.sent``.
"""
