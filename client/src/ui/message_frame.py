"""Single message view — forward, revoke, delete, download.

PLANNED — to be extracted from ``main_frame.py`` (the message bubble and
its details/actions dialogs). Will drive ``MessageService`` and
``ChainService`` for the per-message actions.
"""
