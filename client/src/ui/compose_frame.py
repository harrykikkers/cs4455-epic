"""Compose a new message.

PLANNED — to be extracted from ``main_frame.py`` (the bottom input bar).
Will call ``MessageService.send(recipient_id, plaintext)`` so all crypto
stays in the service/crypto layers.
"""
